<!--components/baseInfo/index.wxml-->
<view class="baseInfo">
   <view class="info-item" wx:for="{{baseInfo}}" wx:key="title">
      <view class="title">{{item.title}}</view>
      <view class="value">{{item.value}}</view>
   </view>
</view>